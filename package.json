{
  "name": "wordguess",
  "version": "0.17.2",
  "private": true,
  "description": "WordGuess - Android wrapper. The game itself is app/ and needs none of this; these packages only build the APK (see PLAN.md M6).",
  "scripts": {
    "serve": "node tools/serve.mjs",
    "data": "node tools/build-data.mjs",
    "android:sync": "cap sync android",
    "android:apk": "cap sync android && cd android && gradlew.bat assembleDebug"
  },
  "dependencies": {
    "@capacitor/android": "^7.0.0",
    "@capacitor/core": "^7.0.0"
  },
  "devDependencies": {
    "@capacitor/assets": "^3.0.5",
    "@capacitor/cli": "^7.0.0"
  }
}
